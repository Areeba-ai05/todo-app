import 'package:flutter/material.dart';

class TodoTask extends StatelessWidget {
  String text;
  TodoTask({super.key, required this.text});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
